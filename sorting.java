import java.io.*;
import java.util.*;
import java.util.Collections.*;

/**
 * Write a description of class sorting here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class sorting
{
    public static void main(String [] args) throws IOException
    {
        Scanner in = new Scanner(new File("sorting.in"));
        
        int numClasses = in.nextInt();
        int count = 1;
        int numStudents, letterCount;
        String blank, name;
        
        while(numClasses != 0)
        {
            numStudents = in.nextInt();
            blank = in.nextLine();
            
            
            
            numClasses--;
            count++;
        }
    }
}